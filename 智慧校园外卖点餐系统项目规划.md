# 智慧校园外卖点餐系统项目规划

> 项目定位：基于 Spring Boot + React 的前后端分离外卖点餐系统。  
> 场景建议：校园食堂 / 校园外卖点餐。  
> 项目目标：在完成课程任务书基本要求的基础上，通过订单状态追踪、数据看板、库存预警、热销推荐等功能提高系统完整度和展示效果。

---

## 1. 项目选题

### 1.1 项目名称

推荐项目名称：

**基于前后端分离架构的智慧校园外卖点餐平台**

也可以简化为：

**智慧校园外卖点餐系统**

### 1.2 项目背景

本系统面向校园食堂外卖点餐场景，主要解决学生在用餐高峰期排队时间长、菜品库存不透明、订单状态不清晰、管理员缺少经营数据统计等问题。

系统支持游客浏览菜品，客户登录后进行点餐和查看订单进度，管理员可以维护菜品、分类、订单和经营数据。项目整体采用前后端分离架构，提高系统的交互体验和开发规范性。

---

## 2. 项目总体目标

本项目的目标不是只做一个简单的“菜品展示 + 下单”页面，而是完成一个具有完整业务闭环的外卖点餐平台。

核心目标包括：

1. 实现游客、客户、管理员三类角色的基本功能。
2. 完成菜品浏览、模糊搜索、购物车、下单、订单状态查询等前台业务。
3. 完成菜品分类管理、菜品管理、订单管理等后台业务。
4. 保证数据来源于数据库，并通过前后端接口进行交互。
5. 增加订单状态时间轴、数据看板、库存预警、热销排行等高分功能。
6. 项目结构清晰，便于后续写课程设计报告和验收展示。

---

## 3. 技术栈规划

### 3.1 后端技术栈

建议使用：

```text
Spring Boot
MyBatis / MyBatis-Plus
MySQL
JWT
Lombok
Maven
```

后端主要负责：

- 用户登录注册
- 权限判断
- 菜品分类管理
- 菜品管理
- 购物车管理
- 订单生成
- 订单状态修改
- 数据统计接口
- 图片上传接口

### 3.2 前端技术栈

建议使用：

```text
React
Vite
Tailwind CSS
TanStack Query
Axios
React Router
ECharts
```

前端主要负责：

- 页面展示
- 路由跳转
- 用户登录状态管理
- 菜品搜索和筛选
- 购物车交互
- 订单状态展示
- 管理员后台页面
- 数据看板可视化

### 3.3 技术说明

虽然课程任务书偏传统 Java Web 技术，如 JSP、Servlet、JDBC、Ajax、JSTL 等，但我们项目可以采用更现代的前后端分离方式实现。

对应关系可以在报告中说明：

| 任务书技术点 | 项目实现方式 |
|---|---|
| Servlet | Spring MVC Controller |
| JavaBean | Entity / DTO / VO |
| JDBC 数据库访问 | MyBatis / MyBatis-Plus |
| Ajax | Axios / TanStack Query |
| 页面数据来自数据库 | 前端通过 REST API 请求后端数据 |
| JavaScript / jQuery | React + JavaScript |

如果老师要求必须体现传统 JSP，可以额外保留一个简单 JSP 页面作为技术点展示，但主体项目建议仍使用 Spring Boot + React。

---

## 4. 角色划分

系统分为三类用户：

### 4.1 游客

游客不需要登录，可以：

- 浏览首页
- 查看菜品分类
- 查看菜品列表
- 模糊搜索菜品
- 查看菜品详情

游客不能：

- 加入购物车
- 提交订单
- 查看订单

### 4.2 客户

客户登录后可以：

- 修改个人信息
- 浏览菜品
- 按分类筛选菜品
- 模糊搜索菜品
- 加入购物车
- 修改购物车数量
- 删除购物车菜品
- 提交订单
- 查看自己的订单
- 查看订单状态进度
- 完成后评价菜品，可选

### 4.3 管理员

管理员登录后可以：

- 查看后台数据看板
- 管理菜品分类
- 管理菜品信息
- 上传菜品图片
- 修改菜品库存和价格
- 上架 / 下架菜品
- 查看全部订单
- 修改订单状态
- 查看销售统计
- 查看库存预警

---

## 5. 必须完成功能点

以下功能属于基本要求，必须完成。

### 5.1 首页与菜品展示

首页需要包含：

- 顶部导航栏
- 菜品分类展示
- 菜品搜索框
- 推荐菜品区域
- 热销菜品区域
- 菜品卡片列表

菜品卡片建议显示：

- 菜品图片
- 菜品名称
- 菜品分类
- 菜品价格
- 库存
- 销量
- 评分，可选
- 加入购物车按钮

### 5.2 菜品模糊查询

游客和客户都可以通过菜名进行模糊查询。

示例：

```text
输入“鸡”
可以查询到：
- 黄焖鸡米饭
- 炸鸡腿套餐
- 香辣鸡排饭
```

后端接口示例：

```text
GET /api/dishes?keyword=鸡
```

### 5.3 用户注册与登录

系统至少支持两类账号：

- 客户
- 管理员

建议普通用户只能注册为客户，管理员账号由数据库预置，避免用户随意注册管理员账号。

登录成功后：

- 客户跳转到前台首页
- 管理员跳转到后台首页

### 5.4 购物车功能

客户登录后可以：

- 添加菜品到购物车
- 修改菜品数量
- 删除购物车项
- 清空购物车
- 自动计算总金额
- 跳转提交订单页面

购物车可以存数据库，也可以用 localStorage。为了系统更完整，建议使用数据库购物车。

### 5.5 提交订单

客户提交订单时需要填写：

- 收货人姓名
- 手机号
- 收货地址
- 订单备注
- 购物车菜品信息

订单提交后，系统需要：

1. 生成订单编号。
2. 生成订单主表数据。
3. 生成订单明细表数据。
4. 扣减菜品库存。
5. 清空购物车。
6. 设置订单初始状态为“待接单”。

### 5.6 查询订单进度

客户可以查看自己的订单列表和订单详情。

订单信息包括：

- 订单编号
- 下单时间
- 订单金额
- 收货地址
- 收货人信息
- 菜品明细
- 订单备注
- 当前状态

订单状态建议设计为：

```text
PENDING      待接单
ACCEPTED     已接单
COOKING      制作中
DELIVERING   配送中
COMPLETED    已完成
CANCELLED    已取消
```

### 5.7 管理员菜品管理

管理员可以：

- 新增菜品
- 查询菜品
- 按分类筛选菜品
- 修改菜品名称
- 修改菜品价格
- 修改菜品库存
- 上传菜品图片
- 修改菜品介绍
- 上架 / 下架菜品

重点规则：

> 已有订单的菜品不能直接删除，只能下架。

实现建议：

不做真正的物理删除，而是给菜品表增加 `status` 字段。

```text
status = 1  上架
status = 0  下架
```

这样既符合要求，也能避免历史订单数据出错。

### 5.8 管理员分类管理

管理员可以：

- 添加分类
- 查看分类
- 修改分类名称
- 删除分类

重点规则：

> 如果分类下已有菜品，则不能删除该分类。

实现逻辑：

```text
删除分类前，先查询 dish 表中是否存在 category_id。
如果存在，则返回提示：该分类下已有菜品，不能删除。
```

### 5.9 管理员订单管理

管理员可以查看所有用户订单。

订单管理页面需要显示：

- 订单编号
- 下单用户
- 下单时间
- 订单金额
- 收货人
- 手机号
- 收货地址
- 菜品明细
- 订单备注
- 当前状态

管理员可以修改订单状态：

```text
待接单 -> 已接单 -> 制作中 -> 配送中 -> 已完成
```

---

## 6. 高分创新功能

创新功能不建议做太复杂的 AI，而是做实现可控、展示效果明显、报告好写的功能。

### 6.1 订单状态时间轴

客户订单详情页显示订单流程：

```text
提交订单 -> 商家接单 -> 制作中 -> 配送中 -> 已完成
```

当前状态高亮。

优点：

- 展示效果好
- 实现难度低
- 非常符合外卖业务场景
- 验收时容易演示

这是最推荐优先完成的创新功能。

### 6.2 管理员数据看板

后台首页展示经营数据。

建议展示：

- 今日订单数
- 今日营业额
- 待处理订单数
- 低库存菜品数量
- 热销菜品 Top 5
- 最近 7 天订单趋势
- 分类销售占比

图表可以使用 ECharts。

建议图表类型：

| 数据 | 图表类型 |
|---|---|
| 最近 7 天订单趋势 | 折线图 |
| 热销菜品 Top 5 | 柱状图 |
| 分类销售占比 | 饼图 |
| 今日数据概览 | 数据卡片 |

### 6.3 热销菜品排行榜

根据订单明细统计菜品销量，首页展示热销榜。

示例：

```text
热销榜 Top 5
1. 香辣鸡腿饭
2. 黄焖鸡米饭
3. 牛肉炒饭
4. 珍珠奶茶
5. 鸡排套餐
```

这个功能可以同时用于首页展示和后台统计。

### 6.4 今日推荐功能

不用做复杂推荐算法，可以采用简单加权规则。

推荐分计算示例：

```text
推荐分 = 销量 * 0.5 + 评分 * 20 + 是否套餐 * 10
```

首页根据推荐分展示“今日推荐”。

报告中可以写：

> 系统根据菜品销量、评分和套餐属性进行简单加权计算，为用户展示推荐菜品，提高用户点餐效率。

### 6.5 库存预警

当菜品库存低于阈值时，在管理员后台显示预警。

例如：

```text
库存 < 10：显示“库存不足”
库存 = 0：显示“已售罄”
```

后台首页可以显示：

```text
低库存菜品数量：3
```

菜品管理表格中可以用标签提示：

```text
库存不足
建议补货
```

### 6.6 快捷备注标签

用户提交订单时可以选择快捷备注：

- 少辣
- 不要香菜
- 多米饭
- 尽快送达
- 需要餐具
- 不需要餐具

这个功能实现简单，但很贴近真实外卖系统。

### 6.7 菜品评价与评分，可选

客户在订单完成后可以评价菜品。

评价内容包括：

- 评分
- 文字评论
- 是否匿名

菜品卡片可以显示平均评分。

如果时间充足可以做；如果时间不够，可以放到“后续改进”部分。

---

## 7. 最终建议功能优先级

### 7.1 第一优先级：必须完成

这些功能必须优先完成：

1. 游客浏览菜品
2. 菜品分类展示
3. 菜名模糊搜索
4. 客户注册登录
5. 客户加入购物车
6. 客户提交订单
7. 客户查看订单进度
8. 管理员登录
9. 管理员分类管理
10. 管理员菜品管理
11. 管理员订单管理
12. 已有订单菜品只能下架不能删除
13. 分类下已有菜品不能删除

### 7.2 第二优先级：高分功能

建议尽量完成：

1. 订单状态时间轴
2. 管理员数据看板
3. 热销菜品排行
4. 库存预警
5. 今日推荐
6. 图片上传
7. 按状态 / 日期 / 菜名筛选

### 7.3 第三优先级：锦上添花

有时间再做：

1. 菜品评价
2. 优惠券
3. 满减活动
4. 订单取消
5. 用户地址簿
6. 暗色模式
7. 响应式移动端页面

---

## 8. 数据库设计初版

### 8.1 用户表 user

```sql
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  role VARCHAR(20) NOT NULL,
  address VARCHAR(255),
  avatar VARCHAR(255),
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 8.2 菜品分类表 category

```sql
CREATE TABLE category (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL,
  sort_order INT DEFAULT 0,
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 8.3 菜品表 dish

```sql
CREATE TABLE dish (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  category_id BIGINT NOT NULL,
  name VARCHAR(100) NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  stock INT NOT NULL DEFAULT 0,
  image_url VARCHAR(255),
  description TEXT,
  status TINYINT DEFAULT 1,
  sales INT DEFAULT 0,
  rating DECIMAL(3,2) DEFAULT 5.00,
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 8.4 购物车表 cart

```sql
CREATE TABLE cart (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 8.5 购物车明细表 cart_item

```sql
CREATE TABLE cart_item (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  cart_id BIGINT NOT NULL,
  dish_id BIGINT NOT NULL,
  quantity INT NOT NULL DEFAULT 1
);
```

### 8.6 订单表 orders

```sql
CREATE TABLE orders (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(50) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  total_amount DECIMAL(10,2) NOT NULL,
  receiver_name VARCHAR(50) NOT NULL,
  receiver_phone VARCHAR(20) NOT NULL,
  receiver_address VARCHAR(255) NOT NULL,
  remark VARCHAR(255),
  status VARCHAR(30) NOT NULL DEFAULT 'PENDING',
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 8.7 订单明细表 order_item

```sql
CREATE TABLE order_item (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_id BIGINT NOT NULL,
  dish_id BIGINT NOT NULL,
  dish_name VARCHAR(100) NOT NULL,
  dish_price DECIMAL(10,2) NOT NULL,
  quantity INT NOT NULL,
  subtotal DECIMAL(10,2) NOT NULL
);
```

### 8.8 评价表 review，可选

```sql
CREATE TABLE review (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_id BIGINT NOT NULL,
  dish_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  rating INT NOT NULL,
  content VARCHAR(500),
  anonymous TINYINT DEFAULT 0,
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 9. 核心数据关系

```text
user 1 - n orders
orders 1 - n order_item
dish 1 - n order_item
category 1 - n dish
user 1 - 1 cart
cart 1 - n cart_item
dish 1 - n cart_item
dish 1 - n review
user 1 - n review
```

---

## 10. 前端页面规划

### 10.1 用户端页面

| 页面 | 路径 | 功能 |
|---|---|---|
| 首页 | `/` | 分类、搜索、推荐、热销 |
| 菜品列表 | `/dishes` | 筛选、搜索、排序 |
| 菜品详情 | `/dishes/:id` | 图片、价格、库存、介绍 |
| 购物车 | `/cart` | 数量修改、总价计算 |
| 提交订单 | `/checkout` | 地址、手机号、备注 |
| 我的订单 | `/orders` | 查看订单列表 |
| 订单详情 | `/orders/:id` | 状态时间轴、订单明细 |
| 登录 | `/login` | 用户登录 |
| 注册 | `/register` | 用户注册 |

### 10.2 管理员页面

| 页面 | 路径 | 功能 |
|---|---|---|
| 后台首页 | `/admin/dashboard` | 数据看板 |
| 菜品管理 | `/admin/dishes` | 新增、修改、上下架、查询 |
| 分类管理 | `/admin/categories` | 分类增删改查 |
| 订单管理 | `/admin/orders` | 查看订单、修改状态 |
| 库存预警 | `/admin/stock` | 查看低库存菜品 |
| 用户管理 | `/admin/users` | 可选 |

---

## 11. 后端接口规划

### 11.1 用户认证接口

```text
POST /api/auth/register
POST /api/auth/login
GET  /api/auth/me
```

### 11.2 菜品接口

```text
GET    /api/dishes
GET    /api/dishes/{id}
POST   /api/admin/dishes
PUT    /api/admin/dishes/{id}
PATCH  /api/admin/dishes/{id}/status
DELETE /api/admin/dishes/{id}
```

说明：

- `GET /api/dishes` 支持 keyword、categoryId、sort 等参数。
- `DELETE /api/admin/dishes/{id}` 需要判断该菜品是否已有订单。
- 如果已有订单，则不允许删除，只能下架。

### 11.3 分类接口

```text
GET    /api/categories
POST   /api/admin/categories
PUT    /api/admin/categories/{id}
DELETE /api/admin/categories/{id}
```

说明：

- 删除分类前需要判断该分类下是否已有菜品。
- 如果已有菜品，则不允许删除。

### 11.4 购物车接口

```text
GET    /api/cart
POST   /api/cart/items
PUT    /api/cart/items/{id}
DELETE /api/cart/items/{id}
DELETE /api/cart
```

### 11.5 订单接口

```text
POST  /api/orders
GET   /api/orders/my
GET   /api/orders/{id}
GET   /api/admin/orders
PATCH /api/admin/orders/{id}/status
```

### 11.6 数据看板接口

```text
GET /api/admin/statistics/overview
GET /api/admin/statistics/sales-trend
GET /api/admin/statistics/top-dishes
GET /api/admin/statistics/category-sales
```

### 11.7 文件上传接口

```text
POST /api/upload/image
```

用于上传菜品图片。

---

## 12. 推荐项目目录结构

### 12.1 后端目录结构

```text
backend
├─ src
│  ├─ main
│  │  ├─ java
│  │  │  └─ com.example.foodorder
│  │  │     ├─ controller
│  │  │     ├─ service
│  │  │     ├─ service.impl
│  │  │     ├─ mapper
│  │  │     ├─ entity
│  │  │     ├─ dto
│  │  │     ├─ vo
│  │  │     ├─ config
│  │  │     ├─ common
│  │  │     └─ utils
│  │  └─ resources
│  │     ├─ mapper
│  │     ├─ application.yml
│  │     └─ static
│  └─ test
└─ pom.xml
```

### 12.2 前端目录结构

```text
frontend
├─ src
│  ├─ api
│  ├─ assets
│  ├─ components
│  ├─ hooks
│  ├─ layouts
│  ├─ pages
│  │  ├─ client
│  │  └─ admin
│  ├─ router
│  ├─ stores
│  ├─ utils
│  ├─ App.jsx
│  └─ main.jsx
├─ index.html
├─ package.json
├─ tailwind.config.js
└─ vite.config.js
```

---

## 13. 页面风格建议

建议整体风格使用：

```text
Cream + Orange + Soft Brown
```

颜色建议：

```text
背景色：#FFF8EE
主色：#F97316
深色文字：#2B2118
卡片背景：#FFFFFF
边框色：#F3E4D2
辅助绿色：#22C55E
```

设计方向：

- 不要做得太像普通后台模板。
- 前台要有餐饮感和校园生活感。
- 使用大圆角卡片。
- 菜品图片要占据较大视觉空间。
- 价格用橙色突出。
- 状态标签要清晰。
- 后台以浅色表格、数据卡片、图表为主。

---

## 14. 开发顺序建议

### 第一阶段：基础框架

先完成：

1. 后端 Spring Boot 项目初始化。
2. 前端 React + Vite + Tailwind 项目初始化。
3. MySQL 数据库连接。
4. 用户表、分类表、菜品表建表。
5. 登录注册接口。
6. 前端登录注册页面。

### 第二阶段：菜品和分类

完成：

1. 管理员分类管理。
2. 管理员菜品管理。
3. 图片上传。
4. 前台菜品列表。
5. 菜品分类筛选。
6. 菜品模糊搜索。

### 第三阶段：购物车和订单

完成：

1. 加入购物车。
2. 修改购物车数量。
3. 提交订单。
4. 扣减库存。
5. 清空购物车。
6. 用户查看订单。
7. 管理员查看订单。

### 第四阶段：订单状态闭环

完成：

1. 管理员修改订单状态。
2. 客户查看订单状态。
3. 订单状态时间轴。
4. 订单状态筛选。

### 第五阶段：高分功能

完成：

1. 数据看板。
2. 热销菜品榜。
3. 库存预警。
4. 今日推荐。
5. 快捷备注标签。
6. 页面美化和响应式适配。

### 第六阶段：验收与报告

完成：

1. 测试所有业务流程。
2. 准备演示账号。
3. 截图整理。
4. 绘制用例图、流程图、ER 图。
5. 编写课程设计报告。
6. 准备验收展示流程。

---

## 15. 验收演示流程

建议验收时按照下面顺序展示：

1. 游客进入首页。
2. 展示菜品分类。
3. 使用搜索框模糊查询菜品。
4. 注册或登录客户账号。
5. 将菜品加入购物车。
6. 修改购物车数量。
7. 提交订单。
8. 查看订单状态时间轴。
9. 切换管理员账号登录。
10. 进入后台数据看板。
11. 查看刚才提交的新订单。
12. 管理员修改订单状态为“制作中”或“配送中”。
13. 回到客户订单详情页查看状态变化。
14. 管理员查看菜品库存变化。
15. 展示低库存预警。
16. 展示已有订单菜品不能删除，只能下架。
17. 展示分类下已有菜品不能删除。

这个流程能够完整体现系统业务闭环。

---

## 16. 报告中可以重点写的内容

课程设计报告可以重点写：

1. 系统开发背景和意义。
2. 系统需求分析。
3. 用户角色分析。
4. 系统功能模块设计。
5. 数据库概念设计和逻辑设计。
6. 前后端分离架构设计。
7. 主要接口设计。
8. 菜品管理模块实现。
9. 购物车与订单模块实现。
10. 管理员订单状态管理。
11. 数据看板与库存预警创新点。
12. 调试过程中遇到的问题及解决方法。
13. 系统不足与后续改进。

---

## 17. 小组分工建议

如果是小组完成，可以这样分工：

### 成员 A：后端基础与用户模块

负责：

- Spring Boot 项目搭建
- 数据库连接
- 用户登录注册
- JWT 权限认证
- 用户信息接口

### 成员 B：菜品与分类模块

负责：

- 分类管理接口
- 菜品管理接口
- 图片上传
- 菜品搜索筛选
- 菜品上下架逻辑

### 成员 C：购物车与订单模块

负责：

- 购物车接口
- 下单接口
- 订单明细生成
- 库存扣减
- 订单状态修改

### 成员 D：前端页面与数据看板

负责：

- React 页面搭建
- Tailwind 样式
- 用户端页面
- 管理员后台页面
- ECharts 数据看板
- 订单状态时间轴

如果人数较少，可以合并分工。

---

## 18. 最终推荐版本

最终建议做成：

```text
智慧校园外卖点餐系统
```

核心功能：

- 游客浏览和搜索菜品
- 客户注册登录
- 客户购物车
- 客户提交订单
- 客户订单状态追踪
- 管理员分类管理
- 管理员菜品管理
- 管理员订单管理
- 管理员数据看板
- 库存预警
- 今日推荐
- 热销榜

技术栈：

```text
后端：Spring Boot + MyBatis-Plus + MySQL + JWT
前端：React + Vite + Tailwind CSS + TanStack Query + Axios
图表：ECharts
图片：本地上传 / 静态资源目录
```

项目优势：

1. 必须功能完整覆盖。
2. 有完整业务闭环。
3. 页面展示效果强。
4. 后台功能充实。
5. 创新功能容易体现。
6. 报告内容好写。
7. 验收演示路线清楚。

---

## 19. 当前最先应该做的事情

建议现在先做这几件事：

1. 确定项目名称。
2. 确定技术栈。
3. 建数据库表。
4. 搭建前后端基础项目。
5. 先完成登录注册。
6. 再完成分类和菜品。
7. 最后完成订单和创新功能。

优先级一定是：

```text
登录注册 -> 菜品分类 -> 菜品管理 -> 前台菜品展示 -> 购物车 -> 下单 -> 订单管理 -> 数据看板
```

不要一开始就做复杂样式和动画，先保证业务流程跑通。
