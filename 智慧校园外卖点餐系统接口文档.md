# 智慧校园外卖点餐系统接口文档

> 项目名称：智慧校园外卖点餐系统  
> 技术架构：Spring Boot + React 前后端分离  
> 接口风格：RESTful API  
> 默认前缀：`/api`  
> 数据格式：`application/json`，文件上传接口除外  

---

## 1. 文档说明

本文档用于前后端接口对接，覆盖用户认证、菜品浏览、分类管理、购物车、订单、管理员后台、数据看板、图片上传和评价等模块。

### 1.1 环境地址

| 环境 | 地址 |
|---|---|
| 本地后端 | `http://localhost:8080` |
| 本地前端 | `http://localhost:5173` |
| API 基础地址 | `http://localhost:8080/api` |

### 1.2 请求头

除注册、登录、游客浏览接口外，其余需要登录的接口都需要携带 JWT。

```http
Authorization: Bearer <token>
Content-Type: application/json
```

文件上传接口使用：

```http
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

### 1.3 通用响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 1.4 分页响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 100,
    "page": 1,
    "pageSize": 10,
    "pages": 10
  }
}
```

### 1.5 通用错误码

| code | 说明 |
|---|---|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或 token 无效 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 409 | 数据冲突，例如用户名已存在、库存不足 |
| 500 | 服务器内部错误 |

### 1.6 角色说明

| 角色 | role | 说明 |
|---|---|---|
| 游客 | 无 | 可浏览分类、菜品、热销和推荐 |
| 客户 | `CUSTOMER` | 可购物车、下单、查看个人订单 |
| 管理员 | `ADMIN` | 可维护分类、菜品、订单和数据看板 |

### 1.7 订单状态枚举

| 状态值 | 中文说明 |
|---|---|
| `PENDING` | 待接单 |
| `ACCEPTED` | 已接单 |
| `COOKING` | 制作中 |
| `DELIVERING` | 配送中 |
| `COMPLETED` | 已完成 |
| `CANCELLED` | 已取消 |

---

## 2. 接口总览

### 2.1 用户认证

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/auth/register` | 游客 | 用户注册 |
| POST | `/auth/login` | 游客 | 用户登录 |
| GET | `/auth/me` | 客户 / 管理员 | 获取当前登录用户 |
| PUT | `/users/me` | 客户 / 管理员 | 修改个人信息 |

### 2.2 菜品与分类

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/categories` | 游客 | 查询分类列表 |
| GET | `/dishes` | 游客 | 查询菜品列表 |
| GET | `/dishes/{id}` | 游客 | 查询菜品详情 |
| GET | `/dishes/top` | 游客 | 查询热销菜品 |
| GET | `/dishes/recommend` | 游客 | 查询今日推荐 |

### 2.3 购物车

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/cart` | 客户 | 查询购物车 |
| POST | `/cart/items` | 客户 | 添加购物车项 |
| PUT | `/cart/items/{id}` | 客户 | 修改购物车项数量 |
| DELETE | `/cart/items/{id}` | 客户 | 删除购物车项 |
| DELETE | `/cart` | 客户 | 清空购物车 |

### 2.4 订单

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/orders` | 客户 | 提交订单 |
| GET | `/orders/my` | 客户 | 查询我的订单 |
| GET | `/orders/{id}` | 客户 / 管理员 | 查询订单详情 |
| PATCH | `/orders/{id}/cancel` | 客户 | 取消订单 |

### 2.5 管理员后台

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/admin/categories` | 管理员 | 新增分类 |
| PUT | `/admin/categories/{id}` | 管理员 | 修改分类 |
| DELETE | `/admin/categories/{id}` | 管理员 | 删除分类 |
| POST | `/admin/dishes` | 管理员 | 新增菜品 |
| PUT | `/admin/dishes/{id}` | 管理员 | 修改菜品 |
| PATCH | `/admin/dishes/{id}/status` | 管理员 | 上架 / 下架菜品 |
| DELETE | `/admin/dishes/{id}` | 管理员 | 删除菜品 |
| GET | `/admin/orders` | 管理员 | 查询全部订单 |
| PATCH | `/admin/orders/{id}/status` | 管理员 | 修改订单状态 |

### 2.6 统计与上传

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/admin/statistics/overview` | 管理员 | 数据概览 |
| GET | `/admin/statistics/sales-trend` | 管理员 | 销售趋势 |
| GET | `/admin/statistics/top-dishes` | 管理员 | 热销菜品 |
| GET | `/admin/statistics/category-sales` | 管理员 | 分类销售占比 |
| GET | `/admin/statistics/stock-alert` | 管理员 | 库存预警 |
| POST | `/upload/image` | 管理员 | 上传图片 |

---

## 3. 用户认证接口

### 3.1 用户注册

```http
POST /api/auth/register
```

普通用户只能注册为客户，管理员账号建议由数据库预置。

请求参数：

```json
{
  "username": "student01",
  "password": "123456",
  "phone": "13800138000",
  "address": "东区宿舍 3 栋 502"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "student01",
    "phone": "13800138000",
    "role": "CUSTOMER",
    "address": "东区宿舍 3 栋 502"
  }
}
```

业务规则：

- `username` 不能为空且不能重复。
- `password` 长度建议不少于 6 位。
- 密码入库前必须加密，不允许明文保存。

### 3.2 用户登录

```http
POST /api/auth/login
```

请求参数：

```json
{
  "username": "student01",
  "password": "123456"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.xxx",
    "user": {
      "id": 1,
      "username": "student01",
      "phone": "13800138000",
      "role": "CUSTOMER",
      "address": "东区宿舍 3 栋 502"
    }
  }
}
```

业务规则：

- 登录成功后前端保存 `token`。
- 客户跳转前台首页，管理员跳转后台首页。

### 3.3 获取当前用户

```http
GET /api/auth/me
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "student01",
    "phone": "13800138000",
    "role": "CUSTOMER",
    "address": "东区宿舍 3 栋 502",
    "avatar": "/uploads/avatar/default.png"
  }
}
```

### 3.4 修改个人信息

```http
PUT /api/users/me
```

请求参数：

```json
{
  "phone": "13900139000",
  "address": "西区宿舍 6 栋 301",
  "avatar": "/uploads/avatar/user-1.png"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

---

## 4. 分类接口

### 4.1 查询分类列表

```http
GET /api/categories
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "盖浇饭",
      "sortOrder": 1,
      "dishCount": 12
    },
    {
      "id": 2,
      "name": "饮品",
      "sortOrder": 2,
      "dishCount": 8
    }
  ]
}
```

### 4.2 新增分类

```http
POST /api/admin/categories
```

请求参数：

```json
{
  "name": "小吃",
  "sortOrder": 3
}
```

响应示例：

```json
{
  "code": 200,
  "message": "新增成功",
  "data": {
    "id": 3,
    "name": "小吃",
    "sortOrder": 3
  }
}
```

### 4.3 修改分类

```http
PUT /api/admin/categories/{id}
```

请求参数：

```json
{
  "name": "特色小吃",
  "sortOrder": 3
}
```

响应示例：

```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

### 4.4 删除分类

```http
DELETE /api/admin/categories/{id}
```

响应示例：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

业务规则：

- 如果分类下已有菜品，则不能删除。
- 删除失败时返回：

```json
{
  "code": 409,
  "message": "该分类下已有菜品，不能删除",
  "data": null
}
```

---

## 5. 菜品接口

### 5.1 查询菜品列表

```http
GET /api/dishes
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `keyword` | string | 否 | 菜名模糊搜索 |
| `categoryId` | number | 否 | 分类 ID |
| `status` | number | 否 | 菜品状态，游客端默认只查上架 |
| `sort` | string | 否 | 排序方式：`sales`、`price_asc`、`price_desc`、`rating` |
| `page` | number | 否 | 页码，默认 1 |
| `pageSize` | number | 否 | 每页数量，默认 10 |

示例：

```http
GET /api/dishes?keyword=鸡&categoryId=1&page=1&pageSize=10
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "categoryId": 1,
        "categoryName": "盖浇饭",
        "name": "黄焖鸡米饭",
        "price": 18.00,
        "stock": 32,
        "imageUrl": "/uploads/dishes/huangmenji.jpg",
        "description": "鸡肉鲜嫩，配米饭",
        "status": 1,
        "sales": 156,
        "rating": 4.8
      }
    ],
    "total": 1,
    "page": 1,
    "pageSize": 10,
    "pages": 1
  }
}
```

### 5.2 查询菜品详情

```http
GET /api/dishes/{id}
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "categoryId": 1,
    "categoryName": "盖浇饭",
    "name": "黄焖鸡米饭",
    "price": 18.00,
    "stock": 32,
    "imageUrl": "/uploads/dishes/huangmenji.jpg",
    "description": "鸡肉鲜嫩，配米饭",
    "status": 1,
    "sales": 156,
    "rating": 4.8,
    "createdTime": "2026-06-02 10:00:00",
    "updatedTime": "2026-06-02 10:00:00"
  }
}
```

### 5.3 查询热销菜品

```http
GET /api/dishes/top?limit=5
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "黄焖鸡米饭",
      "price": 18.00,
      "imageUrl": "/uploads/dishes/huangmenji.jpg",
      "sales": 156
    }
  ]
}
```

### 5.4 查询今日推荐

```http
GET /api/dishes/recommend?limit=6
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 2,
      "name": "香辣鸡排饭",
      "price": 16.00,
      "imageUrl": "/uploads/dishes/jipai.jpg",
      "sales": 98,
      "rating": 4.9,
      "recommendScore": 156.0
    }
  ]
}
```

### 5.5 新增菜品

```http
POST /api/admin/dishes
```

请求参数：

```json
{
  "categoryId": 1,
  "name": "牛肉炒饭",
  "price": 15.00,
  "stock": 50,
  "imageUrl": "/uploads/dishes/beef-rice.jpg",
  "description": "牛肉粒搭配米饭大火翻炒",
  "status": 1
}
```

响应示例：

```json
{
  "code": 200,
  "message": "新增成功",
  "data": {
    "id": 10
  }
}
```

### 5.6 修改菜品

```http
PUT /api/admin/dishes/{id}
```

请求参数：

```json
{
  "categoryId": 1,
  "name": "招牌牛肉炒饭",
  "price": 16.00,
  "stock": 80,
  "imageUrl": "/uploads/dishes/beef-rice.jpg",
  "description": "招牌炒饭，牛肉分量升级",
  "status": 1
}
```

响应示例：

```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

### 5.7 上架或下架菜品

```http
PATCH /api/admin/dishes/{id}/status
```

请求参数：

```json
{
  "status": 0
}
```

响应示例：

```json
{
  "code": 200,
  "message": "状态修改成功",
  "data": null
}
```

状态说明：

| status | 说明 |
|---|---|
| 1 | 上架 |
| 0 | 下架 |

### 5.8 删除菜品

```http
DELETE /api/admin/dishes/{id}
```

业务规则：

- 如果菜品没有历史订单，可以物理删除。
- 如果菜品已有订单，不允许物理删除，只能下架。

删除失败示例：

```json
{
  "code": 409,
  "message": "该菜品已有订单记录，不能删除，请改为下架",
  "data": null
}
```

---

## 6. 购物车接口

### 6.1 查询购物车

```http
GET /api/cart
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "dishId": 1,
        "dishName": "黄焖鸡米饭",
        "price": 18.00,
        "quantity": 2,
        "stock": 32,
        "imageUrl": "/uploads/dishes/huangmenji.jpg",
        "subtotal": 36.00
      }
    ],
    "totalAmount": 36.00,
    "totalQuantity": 2
  }
}
```

### 6.2 添加购物车项

```http
POST /api/cart/items
```

请求参数：

```json
{
  "dishId": 1,
  "quantity": 2
}
```

响应示例：

```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

业务规则：

- 菜品必须处于上架状态。
- 添加数量不能超过当前库存。
- 如果购物车中已存在该菜品，则累加数量。

### 6.3 修改购物车数量

```http
PUT /api/cart/items/{id}
```

请求参数：

```json
{
  "quantity": 3
}
```

响应示例：

```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

### 6.4 删除购物车项

```http
DELETE /api/cart/items/{id}
```

响应示例：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 6.5 清空购物车

```http
DELETE /api/cart
```

响应示例：

```json
{
  "code": 200,
  "message": "清空成功",
  "data": null
}
```

---

## 7. 订单接口

### 7.1 提交订单

```http
POST /api/orders
```

请求参数：

```json
{
  "receiverName": "张三",
  "receiverPhone": "13800138000",
  "receiverAddress": "东区宿舍 3 栋 502",
  "remark": "少辣，不要香菜"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "下单成功",
  "data": {
    "orderId": 1,
    "orderNo": "OD202606021230001",
    "totalAmount": 36.00,
    "status": "PENDING"
  }
}
```

业务规则：

- 从当前登录用户购物车生成订单。
- 购物车不能为空。
- 提交订单时校验菜品库存。
- 订单生成后扣减库存、增加销量、清空购物车。
- 订单初始状态为 `PENDING`。

### 7.2 查询我的订单

```http
GET /api/orders/my
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `status` | string | 否 | 订单状态 |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "orderNo": "OD202606021230001",
        "totalAmount": 36.00,
        "status": "PENDING",
        "statusText": "待接单",
        "createdTime": "2026-06-02 12:30:00",
        "itemCount": 2
      }
    ],
    "total": 1,
    "page": 1,
    "pageSize": 10,
    "pages": 1
  }
}
```

### 7.3 查询订单详情

```http
GET /api/orders/{id}
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "orderNo": "OD202606021230001",
    "userId": 1,
    "username": "student01",
    "totalAmount": 36.00,
    "receiverName": "张三",
    "receiverPhone": "13800138000",
    "receiverAddress": "东区宿舍 3 栋 502",
    "remark": "少辣，不要香菜",
    "status": "PENDING",
    "statusText": "待接单",
    "createdTime": "2026-06-02 12:30:00",
    "updatedTime": "2026-06-02 12:30:00",
    "items": [
      {
        "id": 1,
        "dishId": 1,
        "dishName": "黄焖鸡米饭",
        "dishPrice": 18.00,
        "quantity": 2,
        "subtotal": 36.00
      }
    ],
    "timeline": [
      {
        "status": "PENDING",
        "label": "提交订单",
        "active": true,
        "time": "2026-06-02 12:30:00"
      },
      {
        "status": "ACCEPTED",
        "label": "商家接单",
        "active": false,
        "time": null
      }
    ]
  }
}
```

权限规则：

- 客户只能查看自己的订单。
- 管理员可以查看全部订单。

### 7.4 取消订单

```http
PATCH /api/orders/{id}/cancel
```

请求参数：

```json
{
  "reason": "临时有事，取消订单"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "取消成功",
  "data": null
}
```

业务规则：

- 建议仅 `PENDING` 状态允许客户取消。
- 取消后恢复菜品库存。

### 7.5 管理员查询全部订单

```http
GET /api/admin/orders
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `keyword` | string | 否 | 订单号、用户名、手机号模糊搜索 |
| `status` | string | 否 | 订单状态 |
| `startDate` | string | 否 | 开始日期，格式 `yyyy-MM-dd` |
| `endDate` | string | 否 | 结束日期，格式 `yyyy-MM-dd` |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "orderNo": "OD202606021230001",
        "username": "student01",
        "totalAmount": 36.00,
        "receiverName": "张三",
        "receiverPhone": "13800138000",
        "receiverAddress": "东区宿舍 3 栋 502",
        "status": "PENDING",
        "statusText": "待接单",
        "createdTime": "2026-06-02 12:30:00"
      }
    ],
    "total": 1,
    "page": 1,
    "pageSize": 10,
    "pages": 1
  }
}
```

### 7.6 管理员修改订单状态

```http
PATCH /api/admin/orders/{id}/status
```

请求参数：

```json
{
  "status": "ACCEPTED"
}
```

响应示例：

```json
{
  "code": 200,
  "message": "订单状态修改成功",
  "data": null
}
```

业务规则：

- 正常状态流转为：`PENDING -> ACCEPTED -> COOKING -> DELIVERING -> COMPLETED`。
- 已完成订单不能再修改为其他状态。
- 已取消订单不能再修改为其他状态。

---

## 8. 数据看板接口

### 8.1 数据概览

```http
GET /api/admin/statistics/overview
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "todayOrderCount": 28,
    "todayRevenue": 1250.50,
    "pendingOrderCount": 6,
    "lowStockDishCount": 3,
    "totalUserCount": 120,
    "totalDishCount": 45
  }
}
```

### 8.2 最近 7 天销售趋势

```http
GET /api/admin/statistics/sales-trend?days=7
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "date": "2026-05-27",
      "orderCount": 20,
      "revenue": 860.00
    },
    {
      "date": "2026-05-28",
      "orderCount": 26,
      "revenue": 1024.00
    }
  ]
}
```

### 8.3 热销菜品统计

```http
GET /api/admin/statistics/top-dishes?limit=5
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "dishId": 1,
      "dishName": "黄焖鸡米饭",
      "sales": 156,
      "revenue": 2808.00
    }
  ]
}
```

### 8.4 分类销售占比

```http
GET /api/admin/statistics/category-sales
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "categoryId": 1,
      "categoryName": "盖浇饭",
      "sales": 320,
      "revenue": 5680.00,
      "percent": 45.6
    }
  ]
}
```

### 8.5 库存预警

```http
GET /api/admin/statistics/stock-alert?threshold=10
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "dishId": 8,
      "dishName": "珍珠奶茶",
      "stock": 6,
      "threshold": 10,
      "status": "LOW_STOCK",
      "statusText": "库存不足"
    },
    {
      "dishId": 9,
      "dishName": "鸡排套餐",
      "stock": 0,
      "threshold": 10,
      "status": "SOLD_OUT",
      "statusText": "已售罄"
    }
  ]
}
```

---

## 9. 文件上传接口

### 9.1 上传菜品图片

```http
POST /api/upload/image
```

请求类型：`multipart/form-data`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `file` | File | 是 | 图片文件 |

响应示例：

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "url": "/uploads/dishes/20260602123000.jpg",
    "fileName": "20260602123000.jpg"
  }
}
```

业务规则：

- 建议只允许 `jpg`、`jpeg`、`png`、`webp`。
- 建议单个文件不超过 5MB。
- 返回的 `url` 用于新增或修改菜品时保存到 `imageUrl` 字段。

---

## 10. 评价接口，可选

### 10.1 提交评价

```http
POST /api/reviews
```

请求参数：

```json
{
  "orderId": 1,
  "dishId": 1,
  "rating": 5,
  "content": "味道很好，配送也很快",
  "anonymous": false
}
```

响应示例：

```json
{
  "code": 200,
  "message": "评价成功",
  "data": null
}
```

业务规则：

- 只有订单状态为 `COMPLETED` 时允许评价。
- 用户只能评价自己的订单。
- 同一个订单中的同一道菜建议只允许评价一次。

### 10.2 查询菜品评价

```http
GET /api/dishes/{id}/reviews?page=1&pageSize=10
```

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "username": "匿名用户",
        "rating": 5,
        "content": "味道很好，配送也很快",
        "createdTime": "2026-06-02 13:20:00"
      }
    ],
    "total": 1,
    "page": 1,
    "pageSize": 10,
    "pages": 1
  }
}
```

---

## 11. 前端调用建议

### 11.1 Axios 基础配置

```js
import axios from "axios";

const request = axios.create({
  baseURL: "http://localhost:8080/api",
  timeout: 10000
});

request.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default request;
```

### 11.2 登录状态处理

- 登录成功后保存 `token` 和用户信息。
- 退出登录时清除 `token` 和用户信息。
- 收到 `401` 时跳转登录页。
- 收到 `403` 时提示无权限访问。

### 11.3 金额字段处理

- 后端数据库金额使用 `DECIMAL(10,2)`。
- 前端显示金额时统一保留两位小数。
- 不建议前端自行计算最终订单金额，最终金额以后端为准。

---

## 12. 核心业务规则汇总

1. 游客只能浏览分类、菜品、热销和推荐，不能加入购物车或下单。
2. 普通注册用户默认角色为 `CUSTOMER`。
3. 管理员账号由数据库预置，避免开放注册管理员。
4. 菜品下架后不在游客端展示，但历史订单仍保留菜品快照信息。
5. 已有订单记录的菜品不能物理删除，只能下架。
6. 分类下存在菜品时不能删除分类。
7. 下单时必须重新校验库存，防止库存不足。
8. 订单生成后需要扣减库存、增加销量、清空购物车。
9. 客户只能查看和操作自己的订单。
10. 管理员可以查看全部订单并修改订单状态。
11. 订单状态必须按规则流转，已完成和已取消订单不能继续流转。
12. 图片上传成功后只返回图片地址，菜品保存时再写入菜品表。

---

## 13. 推荐数据库字段与接口字段对应

| 数据库字段 | 接口字段 | 说明 |
|---|---|---|
| `user.id` | `id` / `userId` | 用户 ID |
| `user.username` | `username` | 用户名 |
| `user.role` | `role` | 用户角色 |
| `category.sort_order` | `sortOrder` | 分类排序 |
| `dish.category_id` | `categoryId` | 菜品分类 ID |
| `dish.image_url` | `imageUrl` | 菜品图片 |
| `dish.created_time` | `createdTime` | 创建时间 |
| `dish.updated_time` | `updatedTime` | 更新时间 |
| `orders.order_no` | `orderNo` | 订单编号 |
| `orders.total_amount` | `totalAmount` | 订单总金额 |
| `orders.receiver_name` | `receiverName` | 收货人 |
| `orders.receiver_phone` | `receiverPhone` | 收货手机号 |
| `orders.receiver_address` | `receiverAddress` | 收货地址 |
| `order_item.dish_name` | `dishName` | 下单时菜品名称快照 |
| `order_item.dish_price` | `dishPrice` | 下单时菜品价格快照 |

---

## 14. Postman 测试顺序建议

1. 注册客户：`POST /api/auth/register`
2. 登录客户：`POST /api/auth/login`
3. 管理员登录：`POST /api/auth/login`
4. 管理员新增分类：`POST /api/admin/categories`
5. 管理员新增菜品：`POST /api/admin/dishes`
6. 游客查询分类：`GET /api/categories`
7. 游客查询菜品：`GET /api/dishes`
8. 客户添加购物车：`POST /api/cart/items`
9. 客户查询购物车：`GET /api/cart`
10. 客户提交订单：`POST /api/orders`
11. 客户查询我的订单：`GET /api/orders/my`
12. 管理员修改订单状态：`PATCH /api/admin/orders/{id}/status`
13. 管理员查看数据看板：`GET /api/admin/statistics/overview`

